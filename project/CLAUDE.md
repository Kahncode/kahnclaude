# CLAUDE.md — [Project Name]

> Project instructions for Claude Code. This file is generated by `/kc:generate-claude-md` or can be manually created and enhanced.
> Keep team-shared rules here. Put personal preferences in `CLAUDE.local.md`.

---

## Project Overview

<!-- GUIDE: Brief introduction to the project
Format: 1-2 sentences describing what the project is, who owns it, and its primary purpose
Example: "Backend API for the customer portal. Built in Node.js + Express. Maintained by the platform team."
MANDATORY: Include a link to docs/ARCHITECTURE.md below -->

**Project:** [Project Name]

**Description:** [One-liner describing the project]

**Primary Stack:** [Language/Framework]

**Documentation:** [See docs/ARCHITECTURE.md](docs/ARCHITECTURE.md) — maintained by the documenter agent with system overview, component map, and tech choices.

---

## Critical Rules

### 0. Never Publish Sensitive Data

<!-- GUIDE: Non-negotiable security rule
Format: Bullet list of absolute prohibitions
Always include: passwords, API keys, tokens, .env files, credentials -->

- NEVER commit passwords, API keys, tokens, or secrets
- NEVER commit `.env` files — always verify `.env` is in `.gitignore`
- Before ANY commit: verify no secrets are staged
- Use `security-check` agent to scan for exposed secrets

### 1. Ask Before Deploying

<!-- GUIDE: Operational safety rule
Format: Explicit requirement to wait for user confirmation
Always include this unless the project uses automated deployment with explicit guardrails -->

- NEVER auto-deploy, even if the fix seems simple
- Always wait for explicit confirmation: "yes, deploy this"

### 2. Quality Gates

<!-- GUIDE: Code quality thresholds
Format: Limits on code size, test coverage, or other metrics
Examples: file/function size limits, test pass requirements, linter rules -->

- No file > 300 lines (split if larger)
- No function > 50 lines (extract helpers)
- All tests must pass before committing

### 3. Never Work Directly on Main

<!-- GUIDE: Branching strategy
Format: Show the expected git workflow (branch creation, naming conventions)
Always include: feat/, fix/, docs/, refactor/ branches -->

```bash
# Always branch first — before editing any files
git checkout -b feat/<task-name>
```

- `feat/<name>` — new features
- `fix/<name>` — bug fixes
- `docs/<name>` — documentation
- `refactor/<name>` — refactors

---

## Project-Specific Rules

<!-- GUIDE: Language, framework, and architectural conventions
Format: Grouped rules by category (API, Database, Language, Testing, etc.)
Examples:
  - API Versioning: "All endpoints must use /api/v1/ prefix"
  - Database: "Always use [ORM]. Never import raw drivers"
  - Language: "TypeScript strict mode required. No any types"
  - Testing: "All public functions must have unit tests"
  - Imports: "Use absolute imports from ~/ alias"
-->

[Add your project-specific rules here. Group by category for clarity.]

---

## Tech Stack Details

<!-- GUIDE: Document key architectural decisions
Format: Table with columns: Decision | Choice | Why
Examples:
  - Database | PostgreSQL 15 | ACID transactions, JSON support, proven at scale
  - Framework | Express | Lightweight, unopinionated, team expertise
  - Auth | JWT + httpOnly cookies | Stateless, CSRF protection, mobile-friendly
  - Deployment | Docker + Kubernetes | Scalability, multi-environment support
  - Testing | Jest + Supertest | Fast feedback, snapshot testing, integration testing
-->

| Decision   | Choice                 | Why         |
| ---------- | ---------------------- | ----------- |
| [Category] | [Technology & Version] | [Reasoning] |

---

## When Something Seems Wrong

<!-- GUIDE: Diagnostic checklist for common issues
Format: Symptom → Check → Action
Use this to prevent false-positive bug reports and guide debugging -->

Before assuming something is broken:

- **Missing feature?** → Check feature flags / config BEFORE assuming bug
- **Empty data?** → Check if services are running BEFORE assuming broken
- **Auth failing?** → Check which auth system is active BEFORE debugging
- **Test failing?** → Read the full error message BEFORE changing code
- **Build error?** → Clean cache (`rm -rf node_modules/` or equivalent) BEFORE reporting

---

## Service Ports & Configuration

<!-- GUIDE: Local development environment setup
Format: Table with columns: Service | Port | Environment Variables
Include both standard services and custom ones for this project
Example:
  | API Server | 3000 | PORT=3000, NODE_ENV=development |
  | Database | 5432 | DATABASE_URL=postgres://localhost:5432/mydb |
  | Redis | 6379 | REDIS_URL=redis://localhost:6379 |
-->

| Service   | Dev Port | Environment Variables |
| --------- | -------- | --------------------- |
| [Service] | [port]   | [VAR=value, ...]      |

---

## Project Structure

<!-- GUIDE: ASCII tree showing folder layout with brief descriptions
Format: Tree structure with one-line explanations
Include all top-level directories and key subdirectories
Example:
project/
├── src/
│   ├── api/           - Express routes and controllers
│   ├── models/        - Database models (ORM definitions)
│   ├── middleware/    - Auth, validation, error handling
│   ├── utils/         - Helper functions
│   └── tests/         - Unit and integration tests
├── .claude/           - Claude Code configuration
├── docs/              - Architecture docs (maintained by documenter agent)
├── config/            - Environment and build configs
└── README.md          - Getting started guide
-->

```
project/
├── CLAUDE.md
├── CLAUDE.local.md     (gitignored)
├── .claude/
│   ├── commands/
│   ├── skills/
│   ├── agents/
│   └── hooks/
├── [src/]
├── [tests/]
├── [docs/]
└── ...
```

---

## Key Patterns & Conventions

<!-- GUIDE: Codebase patterns Claude must respect
Format: Bulleted list of recurring design patterns or conventions
Examples:
  - "Factory pattern for service initialization"
  - "All state management via Redux/Context"
  - "Database queries only through repository layer"
  - "Middleware pipeline for request handling"
  - "Event-driven architecture for async operations"
  - "Snake_case for database columns, camelCase for JS"
-->

- [Pattern 1: Description]
- [Pattern 2: Description]
- [Pattern 3: Description]

---

## Code Style

<!-- GUIDE: Tooling and formatting standards
Format: Table with columns: Tool | Configuration / Standard
Include: linter, formatter, type checker, testing framework
Examples:
  | ESLint | extends eslint-config-prettier, rules in .eslintrc.json |
  | Prettier | --print-width 100, --single-quote, configured in .prettierrc |
  | TypeScript | strict mode, tsconfig.json in root |
  | Testing | Jest with 80% coverage threshold |
-->

| Tool           | Configuration      |
| -------------- | ------------------ |
| [Linter]       | [rules/config]     |
| [Formatter]    | [rules/config]     |
| [Type Checker] | [rules/config]     |
| [Testing]      | [framework/config] |

---

## Notes for Claude

<!-- GUIDE: Special instructions for Claude Code agent interactions
Format: Two sections: "Do" (positive actions) and "Don't" (prohibitions)
Examples:
  Do:
    - Use documenter agent to keep docs/ARCHITECTURE.md in sync
    - Ask before refactoring more than 50 lines
    - Suggest performance optimizations with metrics
  Don't:
    - Don't auto-fix linter warnings without asking
    - Don't modify migration files (they are immutable history)
    - Don't commit without running tests first
-->

### Do

- Use the documenter agent to keep `docs/ARCHITECTURE.md` in sync with significant changes
- Ask before refactoring more than 50 lines at once
- Run the full test suite before committing
- Check project structure and patterns BEFORE suggesting changes

### Don't

- Don't auto-commit changes without asking
- Don't modify past migrations or versioned artifacts
- Don't suggest changes across multiple files without explaining the refactoring strategy
- Don't skip the diagnostic checklist ("When Something Seems Wrong") for potential issues
